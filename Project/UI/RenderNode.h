#ifndef BASE_WINDOW_H
#define BASE_WINDOW_H
#include "../Game/BaseObject.h"
class RenderNode : BaseObject{
private:
	
public :
	RenderNode();
	RenderNode(Vec3 pos, Vec3 size, float ang);
};
#endif // !BASE_WINDOW_H
